DELIMITER //

CREATE PROCEDURE GetPatientCountByDisease(
    IN p_disease VARCHAR(100),
    OUT p_count INT
)
BEGIN
    SELECT COUNT(*) INTO p_count
    FROM patient
    WHERE disease = p_disease;
END //

DELIMITER ;